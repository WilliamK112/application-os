- ✅ Threaded provider-specific probe wording overrides through shared template builders: `src/lib/jobs/provider-submit-probe-utils.ts` now accepts optional override text per outcome while keeping the same status contract (`failed`/`needs_manual`).
- ✅ Wired overrides into provider clients: `greenhouse-submit-client.ts` and `lever-submit-client.ts` now pass provider-specific message overrides through the shared probe utility path.
- ✅ Added/updated contract tests to lock taxonomy invariance with custom wording: `src/lib/jobs/provider-submit-probe-utils.test.ts` (new override test) plus regex resilience updates in `provider-submit-clients.test.ts` and `external-submit.test.ts`.
- ✅ Validation after wording-override threading in `projects/application-os`: `npm test -- src/lib/jobs/provider-submit-probe-utils.test.ts src/lib/jobs/provider-submit-clients.test.ts src/lib/jobs/external-submit.test.ts` (full suite observed 85/85 passing) and `npm run lint` (clean).
- Next: add an optional provider-level override config module (e.g. `provider-submit-message-config.ts`) so wording customizations are centralized and reusable by future non-probe submit outcomes, then add a small contract test to lock default-vs-override resolution order.
